# 协作规范（CONTRIBUTING）

本文档说明本仓库的分支规范、Issue 流程和代码提交要求，请所有成员在参与协作前仔细阅读。

---

## 一、分支规范

### 主分支
- `main`：稳定版本，**禁止直接 push**，只接受 Pull Request 合并

### 功能分支命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 功能开发 | `feature/描述` | `feature/tactile-sensor-v2` |
| 问题修复 | `fix/描述` | `fix/motor-control-crash` |
| 实验验证 | `exp/描述` | `exp/rl-training-baseline` |
| 文档更新 | `docs/描述` | `docs/update-env-guide` |

### 工作流程
1. 从 `main` 创建新分支
2. 在新分支上完成开发
3. 提交 Pull Request 到 `main`
4. 至少一名其他成员 Review 后合并

---

## 二、Commit 信息规范

请使用以下前缀格式：

```
<类型>: <简短描述>
```

| 类型 | 含义 |
|------|------|
| `feat` | 新增功能或内容 |
| `fix` | 修复问题 |
| `docs` | 文档更新 |
| `data` | 数据文件变动 |
| `exp` | 实验相关 |
| `chore` | 维护性改动（配置、依赖等） |
| `refactor` | 代码重构（不改变功能） |

示例：
```
feat: 添加视触觉传感器数据预处理模块
fix: 修复电机控制在低温下的超时问题
docs: 更新 meeting/2026-04-01-kickoff.md
```

---

## 三、Issue 使用规范

### Issue 标签

| 标签 | 使用场景 |
|------|----------|
| `task` | 明确的工作任务 |
| `bug` | 程序错误或异常 |
| `discussion` | 需要讨论的问题 |
| `question` | 寻求帮助 |
| `enhancement` | 改进建议 |

### Issue 填写要求
- **标题**：简洁描述问题，使用 `【类型】描述` 格式
- **正文**：说明背景、期望结果、截止时间
- **Assignee**：必须指定负责人
- **Milestone**：重要任务关联到对应里程碑

### Issue 生命周期
```
Open → In Progress（在评论中说明已开始）→ 完成后 Close
```

---

## 四、文件存放规范

请严格按照以下目录存放文件，**不要在根目录随意堆放**：

```
meeting/   ← 会议纪要，格式：YYYY-MM-DD-主题.md
docs/      ← 需求、设计、周报等文档
src/       ← 代码，按模块建子目录
data/      ← 数据说明（不上传原始大文件）
reports/   ← 阶段总结和实验报告
```

---

## 五、大文件处理

Gitea 对大文件有限制，请遵守以下规则：

- **模型文件**（>.pth/.pkl 等）：不要上传，在 `data/` 中写说明文档和下载链接
- **训练数据集**：不要上传，提供说明和获取方式
- **实验结果图表**：可以上传，建议放在 `reports/` 下对应的子目录

---

## 六、沟通协作原则

1. 有问题优先开 Issue，保留讨论记录
2. 会议后 24 小时内上传会议纪要到 `meeting/`
3. 发现他人代码问题时，通过 Issue 或 Pull Request Review 反馈，不要直接修改他人分支
4. 紧急问题可在即时通讯工具联系，但事后要在 Issue 中补充记录

---

*本文档由 AIFusion Bot 自动生成。如需修改协作规范，请提 Pull Request 更新本文件。*
